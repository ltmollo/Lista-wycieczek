import { TripFilterByBeginDatePipe } from './trip-filter-by-begin-date.pipe';

describe('TripFilterByBeginDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterByBeginDatePipe();
    expect(pipe).toBeTruthy();
  });
});
