import { TripFilterByEndDatePipe } from './trip-filter-by-end-date.pipe';

describe('TripFilterByEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterByEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
