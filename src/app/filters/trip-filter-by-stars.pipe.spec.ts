import { TripFilterByStarsPipe } from './trip-filter-by-stars.pipe';

describe('TripFilterByStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterByStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
