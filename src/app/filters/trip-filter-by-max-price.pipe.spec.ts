import { TripFilterByMaxPricePipe } from './trip-filter-by-max-price.pipe';

describe('TripFilterByMaxPricePipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterByMaxPricePipe();
    expect(pipe).toBeTruthy();
  });
});
